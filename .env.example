# Copy to .env and fill in the key for whichever provider(s) you use.
# Only the keys for providers actually referenced in config/roles.yaml are required.

GEMINI_API_KEY=
OPENROUTER_API_KEY=
ZENMUX_API_KEY=
OPENCODE_API_KEY=
